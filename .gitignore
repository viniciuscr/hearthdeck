# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Rust build artifacts
/services/target/
# hearthdeck-overlay is deliberately its own standalone Cargo workspace (see
# services/hearthdeck-overlay/Cargo.toml), not a member of services/, so its
# build output lands in its own nested target/ instead of /services/target/.
/services/hearthdeck-overlay/target/

# Local Hearthdeck service state and diagnostics
/data/
/runtime/
/logs/
/tmp/
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.db
*.db-shm
*.db-wal

# Local service configuration and TLS material. Keep deployment templates.
/deploy/systemd/daemon.env
/deploy/systemd/*.env
!/deploy/systemd/*.env.example
/certs/
/tls/
*.pem
*.key
*.p12
*.pfx

# Development environment overrides and coverage output
.env
.env.*
!.env.example
/coverage/lcov.info

# Machine/employer-specific network config for the docker/ test sandbox
# (e.g. an internal PyPI mirror with embedded credentials). See
# docker/pip.conf.example.
/docker/pip.conf
