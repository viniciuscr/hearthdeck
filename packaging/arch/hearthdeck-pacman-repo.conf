#[hearthdeck]
# GitHub Pages hosts this unsigned repository over HTTPS. Replace this with a
# signed repository before treating it as a trusted distribution channel.
[hearthdeck]
SigLevel = Optional TrustAll
Server = https://viniciuscr.github.io/hearthdeck/arch/$arch
