#!/usr/bin/bash
# Start the exact known-good Gamescope Xterm topology, then launch Hearthdeck.
set -Euo pipefail

exec /usr/bin/gamescope --backend drm --force-windows-fullscreen -- \
  /usr/bin/xterm -fa Monospace -fs 16 -e \
  /usr/lib/hearthdeck/hearthdeck-console-bootstrap
