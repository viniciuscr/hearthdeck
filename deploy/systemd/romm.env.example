# Optional override for the existing RomM deployment's compose file.
# The service already uses this path by default. Copy this file to
# ~/.config/hearthdeck/romm.env only when the deployment moves elsewhere.
ROMM_COMPOSE_FILE=/mnt/external/romM/podman-compose.yaml
